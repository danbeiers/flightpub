{
  "name": "flightpub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && node index.js",
    "buildfront": "cd client && npm run build",
    "start": "concurrently \"cd backend && npm start\" \"cd client && npm start\"",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
